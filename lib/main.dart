import 'package:flutter/material.dart';
// import 'chat_view.dart';
// import 'app_bar.dart';
// import 'tab_bar.dart';
import 'text_field.dart';
// import 'date_format.dart';
// import 'image_widget.dart';
// import 'list_tile.dart';
// import 'list_view.dart';
// import 'mapping_list.dart';
// import 'stateful.dart';
// import 'stateless.dart';

void main() {
  // runApp(ExtractWidgetTest());
  // runApp(ImageWidget());
  // runApp(ListTileTest());
  // runApp(ListViewTest());

  // runApp(StateLessView());
  // runApp(StateFullView());
  // runApp(MappingListView());
  // runApp(DateFormatView());
  // runApp(AppBarView());
  // runApp(TabBarViewTest());
  runApp(TextFieldView());
}
